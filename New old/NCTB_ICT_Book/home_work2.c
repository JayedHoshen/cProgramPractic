#include <stdio.h>

int main()
{
    char letter;

    letter = 'x';
    printf("Value stored in ch is %c\n", letter);

    letter = 'y';
    printf("Value stored in ch is %c\n", letter);

    return 0;
}
