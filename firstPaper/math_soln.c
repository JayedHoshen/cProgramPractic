
/*
#include<stdio.h> // Loan calculate third program

int main()
{
    double loan_amount, interest_rate, number_of_years, total_amount, monthly_amount;

    printf("Enter the loan amount: ");
    scanf("%lf", &loan_amount);

    printf("Enter the interest rate: ");
    scanf("%lf", &interest_rate);

    printf("Number of years: ");
    scanf("%lf", number_of_years);

    total_amount = loan_amount + (loan_amount * interest_rate * number_of_years / 100.00);
    monthly_amount = total_amount / (number_of_years * 12);

    printf("Total amount: %lf\n", total_amount);
    printf("Monthly amount: %lf\n", monthly_amount);

    return 0;
}


#include<stdio.h> // second programm

int main()
{
    double a1, a2, b1, b2, c1, c2, d, x, y;

    printf("a1 = ");
    scanf("%lf", &a1);

    printf("a2 = ");
    scanf("%lf", &a2);

    printf("b1 = ");
    scanf("%lf", &b1);

    printf("b2 = ");
    scanf("%lf", &b2);

    printf("c1 = ");
    scanf("%lf", &c1);

    printf("c2 = ");
    scanf("%lf", &c2);

    d = a1 * b2 - a2 * b1;

    if((int)d == 0) {
        printf("Value of x and y can not be determined.\n");
    }

    else {
        x = (b2 * c1 - b1 * c2) / d;
        y = (a1 * c2 - a2 * c1) / d;

        printf("x = %0.2lf, y = %0.2lf\n");
    }

    return 0;
}
#include<stdio.h>// first program

int main()
{
    double x, y, x_plus_y, x_minus_y;

    printf("Enter the value of x + y: ");
    scanf("%lf", &x_plus_y);

    printf("Enter the value of x - y: ");
    scanf("%lf", &x_minus_y);

    x = (x_plus_y + x_minus_y) / 2;
    y = (x_plus_y - x_minus_y) / 2;

    printf("x = %0.2lf, y = %0.3lf\n", x, y);

    return 0;
}

*/
