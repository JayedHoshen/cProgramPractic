// input double type data and print their sum

#include <stdio.h>

int main()
{
    double a, b, sum;

    scanf("%lf", &a);
    scanf("%lf", &b);

    sum = a + b;

    printf("Sum if: %d\n", sum);

    return 0;
}
